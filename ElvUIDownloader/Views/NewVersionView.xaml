<base:BaseView x:Class="ElvUIDownloader.Views.NewVersionView"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:ui="http://schemas.modernwpf.com/2019"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
          xmlns:viewModels="clr-namespace:ElvUIDownloader.ViewModels"
          xmlns:base="clr-namespace:ElvUIDownloader.Views.Base"
          xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
          xmlns:converters="clr-namespace:ElvUIDownloader.Utils.Converters"
          BorderThickness="1"
                         ui:WindowHelper.UseModernWindowStyle="True"
               ui:WindowHelper.FixMaximizedWindow ="False"
          mc:Ignorable="d"
          Icon="../main.ico"
          d:DataContext="{d:DesignInstance viewModels:NewVersionViewModel}"
          WindowStartupLocation="CenterScreen"
          TextElement.FontSize="14"
          ResizeMode="CanMinimize"
          Title="{Binding Title, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
          Height="600" Width="550">
    <base:BaseView.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </base:BaseView.Resources>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
    <Grid IsEnabled="{Binding Path=IsBusy, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, Converter={StaticResource InverseBooleanConverter}}" Margin="5">
        <Grid Grid.Row="0" Margin="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Обновления" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Margin="15"/>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="5" Margin="5 5 5 10">
                <TextBlock Text="{Binding Path=TextData, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            </ScrollViewer>
        </Grid>
    </Grid>
    </Grid>
</base:BaseView>