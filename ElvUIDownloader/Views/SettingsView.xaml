<base:BaseView  xmlns:base="clr-namespace:ElvUIDownloader.Views.Base" x:Class="ElvUIDownloader.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:ElvUIDownloader.ViewModels"
                xmlns:helpers="clr-namespace:ElvUIDownloader.Utils"
             xmlns:converters="clr-namespace:ElvUIDownloader.Utils.Converters"
             mc:Ignorable="d"
                WindowStartupLocation="CenterOwner"
                  ui:WindowHelper.UseModernWindowStyle="True"
             ui:WindowHelper.FixMaximizedWindow ="False"
                Title="{Binding Path=Title, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
               helpers:DialogResultHelper.DialogResult="{Binding DialogResult, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                Width="500"
                Height="700"
                MinWidth="500"
             d:DataContext="{d:DesignInstance viewModels:SettingsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="700" d:DesignWidth="500">
    <base:BaseView.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
    </base:BaseView.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0 0 25 0">
            <StackPanel Orientation="Vertical" CanVerticallyScroll="True">
                <GroupBox Margin="10 0 0 5" Header="Основные">
                    <StackPanel>
                        <TextBlock Margin="0 5 0 5" Text="Тема" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <ComboBox Margin="0 0 0 5" HorizontalAlignment="Stretch" ItemsSource="{Binding Themes}" SelectedItem="{Binding Theme, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <CheckBox Margin="0 0 0 0" IsChecked="{Binding UseSystemAccentColor, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Content="Использовать системный контраcтный цвет"/>
                        <TextBlock Margin="0 5 0 5" Text="Контрастный цвет" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <ListBox ItemsSource="{Binding AccentColors}" SelectedItem="{Binding AccentColor, UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}" MaxHeight="150">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Margin" Value="1"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Rectangle Width="40" Height="40" Fill="{Binding}" Margin="0"/>
                                        <!--<TextBlock Text="{Binding Color}" Margin="5"/>-->
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock Margin="0 5 0 5" Text="Наименование профиля" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <TextBox IsEnabled="False" Margin="0 0 0 0" IsReadOnly="True" Text="{Binding CurrentProfile, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                        <CheckBox IsEnabled="False" Margin="0 0 0 5" IsChecked="{Binding Path=StartMinimized, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" >Запускать в минимизированном виде</CheckBox>
                    </StackPanel>
                </GroupBox>
                <GroupBox Margin="10 0 0 5" Header="Настройка обновления приложения">
                    <StackPanel>
                        <TextBlock Margin="0 0 0 5" Text="Единица измерения периода времени (В секундах, минутах, часах ..)" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <ComboBox Margin="0 0 0 5" HorizontalAlignment="Stretch" ItemsSource="{Binding Types, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding UpdateApplicationSettings.Type, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <TextBlock Margin="0 0 0 5" Text="Интервал времени (зависит от единицы измерения)" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <TextBox Margin="0 0 0 0" HorizontalAlignment="Stretch" Text="{Binding UpdateApplicationSettings.Interval, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, FallbackValue=0, TargetNullValue=0}"/>
                    </StackPanel>
                </GroupBox>
                <GroupBox Margin="10 0 0 5" Header="Настройка обновления аддона">
                    <StackPanel>
                        <TextBlock Margin="0 0 0 5" Text="Единица измерения периода времени (В секундах, минутах, часах ..)" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <ComboBox Margin="0 0 0 5" HorizontalAlignment="Stretch" ItemsSource="{Binding Types, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding UpdateAddonSettings.Type, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <TextBlock Margin="0 0 0 5" Text="Интервал времени (зависит от единицы измерения)" Style="{StaticResource HeaderTextBlockStyle}" FontSize="14"/>
                        <TextBox Margin="0 0 0 0" HorizontalAlignment="Stretch" Text="{Binding UpdateAddonSettings.Interval, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, FallbackValue=0, TargetNullValue=0}"/>
                    </StackPanel>
                </GroupBox>

            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Style="{StaticResource AccentButtonStyle}" Margin="10" Content="{Binding Path=OkButton, UpdateSourceTrigger=PropertyChanged, FallbackValue=Сохранить, TargetNullValue=Сохранить}" Command="{Binding SaveProfileCommand}" />
            <Button Margin="10" Content="Отмена" IsCancel="True" />
        </StackPanel>
    </Grid>
</base:BaseView>
